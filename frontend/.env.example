VITE_API_URL=http://localhost:4000
# Optional: enables live Google Maps tiles. Needs a browser-restricted key
# (Maps JavaScript API, HTTP referrer restrictions) — different from the
# backend's server-only Places API key. Falls back to OpenStreetMap/Leaflet
# when unset.
VITE_GOOGLE_MAPS_API_KEY=
